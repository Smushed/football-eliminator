import React from 'react'

const about = {
    fontSize: '17px',
    textAlign: 'center',
};

const WelcomeMessage = (props) => {
    return (
        <p style={about}>
            TODO
        </p>
    )
};

export default WelcomeMessage;